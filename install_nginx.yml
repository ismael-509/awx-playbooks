---
- name: Installer Nginx localement
  hosts: localhost
  become: true
  tasks:
      - name: Execute the command in remote shell; stdout goes to the specified file on the remote
        ansible.builtin.shell: pwd && ls -al

